# F02-IAM-05 — Final integration evidence

**Scope:** backend issue #14, baseline `931c7e565d51d669f0a1111c947beef25f2ca57c`, branch `test/f02-final-integration`, 2026-09-24. This is evidence for review, **not** a GATE-F02 decision. Issue #15 must remain Pending and issue #81 Blocked. Prior negative evidence in F02-C/F02-D and the F01 corrigendum is preserved unchanged.

## Migration chain and upgrade

Exactly four migrations exist: `20260618010000_init_expanded_flow`, `20260923033000_f02_add_member`, `20260923050000_f02_add_client_registration`, `20260924143000_f02_add_auth_session_v2`. The last three are the F02 chain. Their SHA-256 values, in that order:

| Migration | SHA-256 |
|---|---|
| Member | `09647EB7DACFB3593C723F4B83FA0C234FEA4C70A7E86A88457FD3F0F8960E50` |
| ClientRegistration | `565D6CB9115B29404B6039747F41BC608DF028ED7142337281938AEC51A74404` |
| AuthSession V2 | `15C2CB4D56502DF6DDC6568821249BB61D941078C30AF6FEBC420FFD758B051F` |

SQL inspection: Member creates table/index/FKs and performs deterministic `INSERT ... SELECT` backfill; ClientRegistration creates enum/table/index/FKs; AuthSession creates enums/table and adds nullable/defaulted RefreshToken columns plus indexes/FKs. No `DROP`, `TRUNCATE`, `DELETE`, destructive rename or historical rewrite appears in the F02 SQL. **F02_MIGRATIONS=EXPAND_ONLY.** No new or empty migration was created.

On disposable PostgreSQL 16, `prisma migrate deploy` applied all four to an empty DB; `migrate status` reported current and `migrate diff --from-url ... --to-schema-datamodel prisma/schema.prisma --exit-code` reported no difference. The existing final PostgreSQL suites exercised the migrated relations and the new HTTP suite created Tenant/User/Member/ClientRegistration/AuthSession/RefreshToken V2. These were isolated disposable databases, not SQLite.

For pre-F02 upgrade, the initial historical SQL alone was applied, then an active Tenant, three Users (AGRICULTOR, DIRECTIVA, inactive ADMINISTRADOR), and a V1 RefreshToken were inserted. After marking the initial migration applied, Prisma deployed the three F02 migrations. All three Users retained id, tenant, role, active flag and timestamps. Each Member matched its User for tenantId, userId, role, active flag, createdAt and updatedAt; backfilled Member.id equaled User.id only for those legacy rows. ClientRegistration=0 and AuthSession=0 retrospectively. The legacy refresh retained id, userId, tenantId, tokenHash, expiresAt and createdAt; `contractVersion=1`, `sessionId=NULL`, `consumedAt=NULL`. No token was regenerated.

Two intermediate boundaries also passed. A DB at F02-A with a real Member upgraded through B+C and retained Member=1, ClientRegistration=0, AuthSession=0. A DB at F02-B with a valid ClientRegistration upgraded through C and retained Member=1, ClientRegistration=1, AuthSession=0. Both used exact historical SQL, Prisma `migrate resolve --applied` for already-applied migrations, then `migrate deploy`.

Database downgrade is **NOT_SUPPORTED_BY_DESIGN**: this chain is expand-only and has no down migrations. Application rollback was measured in a separate detached worktree at historical commit `c345690afa95477f11e12c0ff3dece3db5fef196` against the already expanded database. The old Prisma client generated, build passed, app booted, and HTTP V1 login/me/refresh plus ClientRegistration create succeeded. No history or old source was altered.

## Contract and test evidence

The final HTTP/PostgreSQL suite passed **2/2** through real Nest controllers, JWT, Prisma and PostgreSQL. It verifies V1 login/me/refresh/logout, client registration, V2 login and Session creation, bind, current context, stale-role denial, client revocation invalidating the bound Session, second unbound Session survival, cross-user and cross-tenant Plot 404, and forbidden authority fields returning 400. Existing F02-C passed **12/12**, F02-D **25/25**, each on its own PostgreSQL database and without weakening their assertions. The normal suite passed **25 suites / 105 tests**; the three opt-in PostgreSQL suites are separate. The test-level matrix is `docs/testing/F02_FINAL_TEST_MATRIX.md`.

The generated OpenAPI snapshot is `docs/api/openapi-f02.json`; generation and drift guard use the same configuration as runtime Swagger. Baseline hash `0824486E2CB0F01689755389667AF60BB237E6EB1FC7DF3AF97688FA57FCF52D`; final hash `7CF1EB4831ECF5DEBDF8FE6A8BBD8925EF68E941766F08A1DC927B5F1471A864`. No paths added or removed; twelve IAM operations gained contract metadata and inline response schemas. V1 cookie scheme was added, V2 cookie and Bearer retained. `npm run openapi:check` and contract assertions passed. Full structural summary: `docs/evidence/F02_OPENAPI_DIFF.md`.

The threat model is `docs/security/F02_THREAT_MODEL.md` and requirement/decision mapping is `docs/evidence/F02_FINAL_TRACEABILITY.md`. Mobile main was checked at `9e9cb2334eeaa4c710dc87feb7cfa847ea339724`; Mobile PR #61 was merged and #60 completed, so `F02_DEVICE_CLIENT_ID_EVIDENCE.md` is **REUSED_VERIFIED_EVIDENCE**, not a new device test.

## CI, residual risk and decisions

GitHub Actions was absent in the baseline. This branch adds a PostgreSQL 16/Node 22 workflow with isolated databases, normal suite, build, OpenAPI drift/contract check, and all three PostgreSQL suites. PR #93's [first workflow run](https://github.com/Roderich1/Agro_Sindicato_Backend-Web/actions/runs/36066816451) passed (job `verify`, 1m12s); the final evidence-only commit must have its own check verified before review. No SonarCloud check appeared in the PR check rollup at this point; SonarCloud is distinct from GitHub Actions. Local `npm audit` reported **15** advisories (2 low, 3 moderate, 10 high, 0 critical); no `audit fix` or dependency upgrade was performed. This is review debt, not a claim of production exposure.

Residual risks: V1 has no individual AuthSession but revalidates User/Member/Tenant per request; multi-membership selector is DELEGATED; legacy private central contracts remain compatibility surface; bearer theft remains possible until expiry; client backup/restore may duplicate the pseudonymous installation ID. Client ID is neither a secret nor a hardware ID; backend persists its SHA-256, and authorization never relies on it alone. Mobile remote auth, JWT storage, refresh secure storage, remote registration and networking are DELEGATED. Final Sync DTO whitelist, ACK, conflict semantics, ClientRegistration-to-SyncOperation binding and Sync versioning are **DELEGATED_F05**. F06/F07/F09/F10 were not initiated.

No issue was closed, no PR merged, no Gate status set to Passed/Failed/Waived, no #81 work executed, no deploy performed.
